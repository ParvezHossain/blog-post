package com.parvez.blogs.validation;

/**
 * Validation group applied only to LOCAL (username + password) users.
 * Constraints tagged with this group are skipped for OAuth2 users.
 */
public class LocalUserValidation {
}
